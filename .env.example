# Server
PORT=3000
NODE_ENV=development

# Logging
LOG_LEVEL=info          # error | warn | info | http | debug

# Rate limiting
RATE_LIMIT=200          # Max requests per minute per IP

# Generation limits
MAX_COUNT=100           # Max records per request

# CORS
CORS_ORIGIN=*           # Comma-separated origins or * for all
